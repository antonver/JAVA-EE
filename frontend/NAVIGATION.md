# 🧭 Навигация и роутинг

## 📋 Структура

Приложение использует **React Router v6** с **Outlet** для вложенных маршрутов и **Layout** компонент для общей структуры.

---

## 🏗️ Архитектура

```
App.jsx
├── Публичные маршруты (без Layout)
│   ├── /login - Вход
│   └── /register - Регистрация
│
└── Защищенные маршруты (с Layout + Outlet)
    ├── / - Главная страница
    ├── /dashboard - Панель управления
    ├── /profile - Профиль пользователя
    ├── /settings - Настройки
    └── /admin - Админ панель (только для ADMIN)
```

---

## 🎨 Layout компонент

Layout (`src/components/Layout.jsx`) включает:

### 1. **AppBar (навигационная панель)**
- Логотип с переходом на главную
- Меню навигации (десктоп и мобильное)
- Информация о пользователе
- Меню профиля с выпадающим списком

### 2. **Drawer (боковое меню для мобильных)**
- Автоматически открывается на мобильных устройствах
- Список навигационных элементов
- Закрывается после выбора

### 3. **Main контент (Outlet)**
- Здесь рендерятся вложенные маршруты
- Используется `<Outlet />` из React Router

### 4. **Footer (футер)**
- Информация о копирайте

---

## 🚀 Маршруты

### Публичные (для неавторизованных)

#### `/login` - Страница входа
- Форма входа с email и паролем
- После успешного входа → редирект на `/`

#### `/register` - Страница регистрации
- Форма регистрации с email, паролем и именем
- После регистрации → автоматический вход и редирект на `/`

---

### Защищенные (требуют авторизации)

#### `/` (Home) - Главная страница
- **Hero секция** с приветствием
- **Карточки быстрого доступа**:
  - Панель управления
  - Профиль
  - Настройки
- **Информация о системе**

#### `/dashboard` - Панель управления
- **Статистика аккаунта**:
  - Статус аккаунта
  - Роль пользователя
  - Тип безопасности
- **Личная информация**
- **Информация о безопасности**
- **Админ панель** (если роль = ADMIN)

#### `/profile` - Профиль пользователя
- **Аватар** с первой буквой имени
- **Детальная информация**:
  - Полное имя
  - Email
  - Роль
  - Время истечения токена

#### `/settings` - Настройки
- **Переключатели**:
  - Уведомления
  - Темная тема
  - Email уведомления
  - Двухфакторная аутентификация
  - Язык

#### `/admin` - Админ панель
- Доступна только пользователям с ролью **ADMIN**
- Пункт меню появляется автоматически для админов

---

## 🔐 Защита маршрутов

### PrivateRoute
```jsx
const PrivateRoute = ({ children }) => {
  const { isAuthenticated } = useSelector((state) => state.auth);
  return isAuthenticated ? children : <Navigate to="/login" />;
};
```

- Проверяет авторизацию через Redux
- Неавторизованных → редирект на `/login`

### PublicRoute
```jsx
const PublicRoute = ({ children }) => {
  const { isAuthenticated } = useSelector((state) => state.auth);
  return !isAuthenticated ? children : <Navigate to="/" />;
};
```

- Авторизованных → редирект на `/` (главную)

---

## 📱 Адаптивность

### Десктоп (≥ 960px)
- Горизонтальное меню в AppBar
- Полная информация о пользователе
- Все элементы видны

### Мобильные (< 960px)
- Кнопка "гамбургер" для открытия Drawer
- Боковое меню (Drawer)
- Компактный вид

---

## 🎯 Навигационные элементы

### Основное меню (в AppBar)

```javascript
const navigationItems = [
  { title: 'Главная', path: '/', icon: <HomeIcon /> },
  { title: 'Панель управления', path: '/dashboard', icon: <Dashboard /> },
  // Если роль === 'ADMIN':
  { title: 'Администрирование', path: '/admin', icon: <AdminPanelSettings /> },
];
```

### Меню профиля (выпадающее)

- **Профиль** → `/profile`
- **Настройки** → `/settings`
- **Выйти** → logout и редирект на `/login`

---

## 🔄 Outlet - как это работает

### App.jsx
```jsx
<Route path="/" element={<PrivateRoute><Layout /></PrivateRoute>}>
  <Route index element={<Home />} />
  <Route path="dashboard" element={<Dashboard />} />
  <Route path="profile" element={<Profile />} />
  ...
</Route>
```

### Layout.jsx
```jsx
<Box component="main">
  <Container maxWidth="xl">
    <Outlet /> {/* Здесь рендерятся вложенные маршруты */}
  </Container>
</Box>
```

**Результат:**
- При переходе на `/` → рендерится `<Home />`
- При переходе на `/dashboard` → рендерится `<Dashboard />`
- Layout остается неизменным (AppBar, Footer)

---

## 🎨 Особенности навигации

### 1. Подсветка активного маршрута
```jsx
<Button
  sx={{
    backgroundColor: location.pathname === item.path
      ? 'rgba(255, 255, 255, 0.1)'
      : 'transparent',
  }}
>
```

### 2. Динамическое меню в зависимости от роли
```javascript
if (user?.role === 'ADMIN') {
  navigationItems.push({
    title: 'Администрирование',
    path: '/admin',
    icon: <AdminPanelSettings />,
  });
}
```

### 3. Автоматическое закрытие Drawer на мобильных
```javascript
const handleNavigate = (path) => {
  navigate(path);
  if (isMobile) {
    setMobileOpen(false); // Закрываем меню после клика
  }
};
```

### 4. Информация о пользователе в AppBar
```jsx
<Typography variant="body2">{user.fullName}</Typography>
<Typography variant="caption">{user.role}</Typography>
```

---

## 🧪 Пример использования навигации в компоненте

```jsx
import { useNavigate } from 'react-router-dom';

function MyComponent() {
  const navigate = useNavigate();

  const goToDashboard = () => {
    navigate('/dashboard');
  };

  return <Button onClick={goToDashboard}>К панели</Button>;
}
```

---

## 🎨 Компоненты MUI, использованные в навигации

- **AppBar** - Верхняя панель навигации
- **Toolbar** - Контейнер для элементов AppBar
- **Drawer** - Боковое меню
- **List / ListItem** - Списки в меню
- **Menu / MenuItem** - Выпадающее меню
- **IconButton** - Кнопки-иконки
- **Avatar** - Аватар пользователя
- **Container** - Контейнер с ограничением ширины

---

## 📂 Файловая структура

```
src/
├── components/
│   ├── Layout.jsx          # Главный layout с AppBar и Outlet
│   └── UserInfo.jsx        # Компонент информации о пользователе
├── pages/
│   ├── Home.jsx            # Главная страница
│   ├── Dashboard.jsx       # Панель управления
│   ├── Profile.jsx         # Профиль
│   ├── Settings.jsx        # Настройки
│   ├── Login.jsx           # Вход
│   └── Register.jsx        # Регистрация
└── App.jsx                 # Роутинг
```

---

## ✨ Преимущества текущей архитектуры

✅ **Outlet** - один Layout для всех страниц  
✅ **Вложенные маршруты** - чистая структура  
✅ **Защищенные маршруты** - автоматическая проверка авторизации  
✅ **Динамическое меню** - адаптируется под роль пользователя  
✅ **Адаптивность** - отлично работает на мобильных  
✅ **Material-UI** - современный дизайн  
✅ **Подсветка активных ссылок** - понятная навигация  

---

## 🚀 Готово к использованию!

Теперь у вас есть полноценная навигационная система с:
- Красивым AppBar
- Боковым меню для мобильных
- Защищенными маршрутами
- Динамическим меню в зависимости от роли
- Outlet для вложенных маршрутов

