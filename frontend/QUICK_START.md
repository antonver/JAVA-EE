# 🚀 Быстрый старт

## ✅ Что было создано

### 1. **Redux Store** для управления авторизацией
- `src/store/store.js` - главный store
- `src/store/slices/authSlice.js` - slice для auth с async thunks

### 2. **API сервис** с Axios instance
- `src/services/api.js` - настроенный axios instance
- Автоматическое добавление Bearer token
- Обработка ошибок 401

### 3. **JWT утилиты**
- `src/utils/jwtUtils.js` - парсинг и декодирование JWT
- Извлечение роли пользователя из токена
- Проверка истечения токена

### 4. **Страницы**
- `src/pages/Login.jsx` - форма входа
- `src/pages/Register.jsx` - форма регистрации  
- `src/pages/Home.jsx` - главная страница (защищенная)

### 5. **Компоненты**
- `src/components/UserInfo.jsx` - компонент информации о пользователе

### 6. **Роутинг и защита**
- `src/App.jsx` - роутинг с защищенными маршрутами
- `src/main.jsx` - точка входа с Redux Provider

---

## 📦 Установленные пакеты

```
@reduxjs/toolkit - Redux
react-redux - React bindings для Redux
jwt-decode - декодирование JWT
axios - HTTP клиент
react-router-dom - роутинг
```

---

## ⚙️ Настройка

### 1. Создайте файл `.env`

```bash
cd frontend
```

Создайте файл `.env` со следующим содержимым:

```env
VITE_API_URL=http://localhost:8888
```

**Примечание:** Если файл `.env` не создан, приложение будет использовать дефолтный URL `http://localhost:8888`

### 2. Запустите приложение

```bash
npm run dev
```

Откройте http://localhost:5173

---

## 🎯 Как это работает

### Регистрация

1. Пользователь заполняет форму (email, password, fullName)
2. Данные отправляются на `POST /api/auth/signup`
3. Бэкенд возвращает JWT токен: `{ token: "..." }`
4. Токен декодируется и сохраняется в localStorage
5. Из токена извлекается роль и информация о пользователе
6. Redux store обновляется
7. Пользователь перенаправляется на главную страницу

### Вход

1. Пользователь вводит email и password
2. Данные отправляются на `POST /api/auth/login`
3. Бэкенд возвращает JWT токен
4. Токен декодируется и сохраняется
5. Redux store обновляется
6. Редирект на главную

### Защищенные запросы

Все запросы через axios instance автоматически включают:

```
Authorization: Bearer YOUR_JWT_TOKEN
```

### JWT декодирование

Токен автоматически парсится, и из него извлекается:
- **email** - из поля `sub` или `email`
- **role** - из поля `role` или `authorities[0]`
- **fullName** - из поля `fullName` или `name`
- **exp** - время истечения

---

## 📂 Структура Redux State

```javascript
{
  auth: {
    user: {
      email: "user@example.com",
      fullName: "Иван Иванов",
      role: "USER", // или "ADMIN"
      exp: 1234567890
    },
    token: "eyJhbGc...",
    isAuthenticated: true,
    isLoading: false,
    error: null
  }
}
```

---

## 🔐 Использование в компонентах

```javascript
import { useSelector, useDispatch } from 'react-redux';
import { loginUser, logout } from './store/slices/authSlice';

function MyComponent() {
  const dispatch = useDispatch();
  const { user, isAuthenticated } = useSelector((state) => state.auth);

  // Вход
  const handleLogin = () => {
    dispatch(loginUser({ 
      email: 'test@example.com', 
      password: '123456' 
    }));
  };

  // Выход
  const handleLogout = () => {
    dispatch(logout());
  };

  // Проверка роли
  if (user?.role === 'ADMIN') {
    return <AdminPanel />;
  }

  return <div>Обычный пользователь</div>;
}
```

---

## 🛡️ Защищенные маршруты

Маршруты в `App.jsx`:

- `/login` - только для неавторизованных
- `/register` - только для неавторизованных
- `/` - только для авторизованных

Автоматический редирект:
- Если авторизован → пытается зайти на /login → редирект на /
- Если не авторизован → пытается зайти на / → редирект на /login

---

## 🧪 Тестирование

1. Запустите бэкенд (порт 8080)
2. Запустите фронтенд: `npm run dev`
3. Откройте http://localhost:5173
4. Попробуйте зарегистрироваться
5. Проверьте, что токен сохранен (DevTools → Application → LocalStorage)
6. Обновите страницу - вы должны остаться авторизованным
7. Проверьте информацию на главной странице
8. Выйдите - токен удалится

---

## 🎨 Material-UI компоненты

Использованы:
- `TextField` - поля ввода
- `Button` - кнопки
- `Paper` - карточки
- `Alert` - уведомления об ошибках
- `CircularProgress` - индикатор загрузки
- `Chip` - роль пользователя
- `IconButton` - показать/скрыть пароль

---

## 📡 API Endpoints (Backend)

Убедитесь, что ваш бэкенд имеет следующие endpoints:

### Регистрация
```
POST /auth/signup
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123",
  "fullName": "Иван Иванов"
}

Response:
{
  "token": "eyJhbGciOiJIUzI1NiIs..."
}
```

### Вход
```
POST /auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}

Response:
{
  "token": "eyJhbGciOiJIUzI1NiIs..."
}
```

---

## 🔍 Отладка

### Проверить токен в консоли

```javascript
const token = localStorage.getItem('token');
console.log(token);

import { decodeToken } from './utils/jwtUtils';
console.log(decodeToken(token));
```

### Проверить Redux state

Установите Redux DevTools Extension для браузера:
- Chrome: [Redux DevTools](https://chrome.google.com/webstore/detail/redux-devtools)
- Firefox: [Redux DevTools](https://addons.mozilla.org/en-US/firefox/addon/reduxdevtools/)

---

## ⚠️ Важные примечания

1. **Безопасность**: JWT хранится в localStorage (не самый безопасный вариант для production)
2. **CORS**: Убедитесь, что бэкенд разрешает CORS для http://localhost:5173
3. **HTTPS**: В production используйте HTTPS
4. **Refresh tokens**: Для production добавьте механизм обновления токенов

---

## 🎉 Готово!

Система авторизации полностью настроена и готова к использованию!

Для получения дополнительной информации см. `README_AUTH.md`

