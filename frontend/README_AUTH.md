# 🔐 Система Авторизации React + Redux

## 📋 Описание

Полнофункциональная система авторизации с использованием:
- **React** - UI компоненты
- **Redux Toolkit** - управление состоянием
- **Material-UI (MUI)** - дизайн компонентов
- **React Router** - маршрутизация
- **Axios** - HTTP запросы
- **JWT** - декодирование токенов

## 🚀 Установка

Все зависимости уже установлены. Если нужно переустановить:

```bash
cd frontend
npm install
```

## ⚙️ Конфигурация

Создайте файл `.env` в папке `frontend`:

```env
VITE_API_URL=http://localhost:8888
```

## 🏗️ Структура проекта

```
frontend/src/
├── store/
│   ├── store.js              # Redux store
│   └── slices/
│       └── authSlice.js      # Auth slice с actions
├── services/
│   └── api.js                # Axios instance и API методы
├── utils/
│   └── jwtUtils.js           # Утилиты для работы с JWT
├── pages/
│   ├── Login.jsx             # Страница входа
│   ├── Register.jsx          # Страница регистрации
│   └── Home.jsx              # Главная страница
├── App.jsx                   # Главный компонент с роутингом
└── main.jsx                  # Точка входа с Redux Provider
```

## 📡 API Endpoints

### Регистрация
```
POST /auth/signup
Body: {
  "email": "string",
  "password": "string",
  "fullName": "string"
}
Response: { "token": "jwt_token_here" }
```

### Вход
```
POST /auth/login
Body: {
  "email": "string",
  "password": "string"
}
Response: { "token": "jwt_token_here" }
```

## 🔑 JWT Токен

Токен автоматически:
- **Сохраняется** в localStorage
- **Декодируется** для извлечения информации о пользователе
- **Добавляется** к каждому запросу через Axios interceptor
- **Проверяется** на истечение срока

### Структура декодированного токена

```javascript
{
  email: "user@example.com",
  role: "USER" | "ADMIN",
  fullName: "Имя Пользователя",
  exp: 1234567890  // timestamp истечения
}
```

## 🎨 Компоненты

### Login (Вход)
- Email и пароль
- Показ/скрытие пароля
- Обработка ошибок
- Загрузка (loading state)
- Редирект после успешного входа

### Register (Регистрация)
- Email, пароль и полное имя
- Показ/скрытие пароля
- Обработка ошибок
- Загрузка (loading state)
- Редирект после успешной регистрации

### Home (Главная)
- Отображение информации пользователя
- Роль пользователя (из JWT)
- Кнопка выхода

## 🔒 Защищенные маршруты

### Публичные (доступны только неавторизованным)
- `/login` - Вход
- `/register` - Регистрация

### Приватные (требуют авторизации)
- `/` - Главная страница

## 🛠️ Redux Store

### State
```javascript
{
  auth: {
    user: { email, role, fullName, exp },
    token: "jwt_token",
    isAuthenticated: boolean,
    isLoading: boolean,
    error: string | null
  }
}
```

### Actions
- `loginUser(credentials)` - Async thunk для входа
- `registerUser(userData)` - Async thunk для регистрации
- `logoutUser()` - Async thunk для выхода
- `logout()` - Синхронный выход
- `clearError()` - Очистка ошибок

## 📦 Использование в компонентах

```javascript
import { useSelector, useDispatch } from 'react-redux';
import { loginUser, logout } from './store/slices/authSlice';

function MyComponent() {
  const dispatch = useDispatch();
  const { user, isAuthenticated, isLoading } = useSelector((state) => state.auth);

  const handleLogin = () => {
    dispatch(loginUser({ email: '...', password: '...' }));
  };

  const handleLogout = () => {
    dispatch(logout());
  };

  return (
    <div>
      {isAuthenticated && <p>Привет, {user.fullName}!</p>}
      {user?.role === 'ADMIN' && <p>Вы администратор</p>}
    </div>
  );
}
```

## 🔧 API Service

Axios instance автоматически:
- Добавляет Bearer token к заголовкам
- Обрабатывает ошибки 401 (редирект на /login)
- Использует базовый URL из .env

```javascript
import api from './services/api';

// Токен добавляется автоматически
const response = await api.get('/protected-endpoint');
```

## ▶️ Запуск

```bash
cd frontend
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

## 🧪 Тестирование

1. Откройте `http://localhost:5173`
2. Попробуйте зарегистрироваться
3. После регистрации вы будете автоматически авторизованы
4. Проверьте информацию о пользователе на главной странице
5. Выйдите и войдите снова

## 📝 Примечания

- JWT токен хранится в localStorage
- При перезагрузке страницы пользователь остается авторизованным
- Истекшие токены автоматически удаляются
- Все формы имеют валидацию
- Ошибки отображаются через Material-UI Alert

## 🎯 Особенности

✅ Парсинг JWT токена с извлечением роли  
✅ Автоматическое добавление токена к запросам  
✅ Защищенные и публичные маршруты  
✅ Красивый UI с Material-UI  
✅ Redux для централизованного управления состоянием  
✅ TypeScript-ready структура  
✅ Обработка ошибок  
✅ Loading states  
✅ Показ/скрытие пароля  

## 🔄 Обновление API URL

Если ваш бэкенд работает на другом порту или домене, обновите `.env`:

```env
VITE_API_URL=http://localhost:ВАШИ_ПОРТ/api
# или
VITE_API_URL=https://your-domain.com/api
```

После изменения перезапустите dev-сервер.

